@model Habit_Tracker___Doveloop.Models.HabitLabel

@{
    ViewData["Title"] = "Delete";
}
<head>
    <link rel="stylesheet" type="text/css" href="/css/labels.css" />
</head>
<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div class="card">
    <h4>Label</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10 name">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="User" />
        <input type="hidden" asp-for="Name" />
        <input type="hidden" asp-for="Type" value="label" />
        @for(int i = 0; i < (Model.RelationIds != null ? Model.RelationIds.Count : 0); i++)
        {
            @Html.HiddenFor(model => model.RelationIds[i], new { @name = "RelationIds"})
        }
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a class = "interaction-buttons" asp-action="Index">Back to List</a>
    </form>
</div>
